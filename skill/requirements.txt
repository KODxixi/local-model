# local-rag 依赖
# 用 Python 3.13 测试通过

# 核心
lancedb>=0.30.0
pyarrow>=14.0.0
PyYAML>=6.0
Pillow>=10.0.0

# 文件解析
markitdown>=0.1.0
pypdf>=4.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=1.0.0
PyMuPDF>=1.24.0  # fitz，PDF 文本层提取

# HTTP 客户端（标准库 urllib，无需额外依赖）
# MCP server（仅 MCP 层需要）
# mcp>=1.8.0

# 开发依赖（可选）
# pytest>=7.0.0
# ruff>=0.1.0
# mypy>=1.5.0
